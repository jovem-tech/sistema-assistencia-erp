# Release 2.23.0 - Baixa da OS, cartÃµes e cobranÃ§a automÃ¡tica

Data: `06/06/2026`

## Objetivo

Fechar o fluxo profissional de entrega da ordem de serviÃ§o com:

- baixa tÃ©cnica sem sair da listagem;
- recebimento parcial ou total no mesmo fluxo;
- cÃ¡lculo de taxa de cartÃ£o e lucro estimado;
- continuidade de cobranÃ§a quando ainda existir saldo em aberto.

## O que entrou

- modal `Baixa da OS` na listagem `/os`;
- status intermediÃ¡rio `entregue_pagamento_pendente`;
- memÃ³ria de `status_final_pendente_pagamento` para encerramento definitivo apÃ³s quitaÃ§Ã£o;
- trava administrativa para alteraÃ§Ãµes financeiras em OS jÃ¡ concluÃ­das tecnicamente;
- submenu `FinanÃ§as -> CartÃµes e taxas`;
- simulador de venda lÃ­quida por `operadora`, `bandeira`, `modalidade` e `parcelas`;
- despesa automÃ¡tica de `Taxa de cartÃ£o` quando houver recebimento em cartÃ£o;
- fila `os_cobranca_agendamentos` com rÃ©gua de `1`, `3` e `5` dias;
- follow-up opcional de retorno pÃ³s-serviÃ§o;
- comando `php spark os:cobrancas` para processar os agendamentos pendentes.

## Camada de negÃ³cio

- `OsSettlementService` centraliza:
  - custo estimado da OS;
  - criaÃ§Ã£o e sincronizaÃ§Ã£o do tÃ­tulo `A receber`;
  - gravaÃ§Ã£o de metadados de cartÃ£o;
  - criaÃ§Ã£o da despesa da taxa da maquininha;
  - follow-up de retorno;
  - agendamento/cancelamento de cobranÃ§a;
  - encerramento automÃ¡tico da OS quando a quitaÃ§Ã£o financeira chega a zero.
- `FinanceiroCartaoService` centraliza:
  - dataset ativo de operadoras, bandeiras e taxas;
  - simulaÃ§Ã£o de taxa percentual e fixa;
  - valor lÃ­quido e prazo de recebimento.

## Banco de dados

Migration principal:
- `app/Database/Migrations/2026-06-06-120000_CreateOsEncerramentoFinanceiroCartoes.php`

Estruturas criadas/alteradas:
- novas colunas em `os`:
  - `status_final_pendente_pagamento`
  - `baixa_tecnica_em`
  - `baixa_tecnica_por`
- novas tabelas:
  - `financeiro_cartao_operadoras`
  - `financeiro_cartao_bandeiras`
  - `financeiro_cartao_taxas`
  - `financeiro_movimentos_cartao`
  - `os_cobranca_agendamentos`

ObservaÃ§Ã£o tÃ©cnica:
- a primeira execuÃ§Ã£o da migration exigiu ajuste de compatibilidade entre tipos de FK em `os_cobranca_agendamentos`, e a versÃ£o final jÃ¡ foi corrigida e validada com `php spark migrate`.

## Arquivos principais

- `app/Controllers/Os.php`
- `app/Controllers/Financeiro.php`
- `app/Controllers/FinanceiroCartoes.php`
- `app/Services/OsSettlementService.php`
- `app/Services/FinanceiroCartaoService.php`
- `app/Commands/OsCobrancasLifecycle.php`
- `app/Views/os/index.php`
- `app/Views/os/form.php`
- `app/Views/financeiro/cartoes.php`
- `public/assets/js/os-closure-modal.js`

## ValidaÃ§Ã£o executada

- `php -l app/Controllers/Os.php`
- `php -l app/Controllers/Financeiro.php`
- `php -l app/Controllers/FinanceiroCartoes.php`
- `php -l app/Services/OsSettlementService.php`
- `php -l app/Services/FinanceiroCartaoService.php`
- `php -l app/Views/os/index.php`
- `php -l app/Views/os/form.php`
- `php -l app/Views/financeiro/cartoes.php`
- `node --check public/assets/js/os-closure-modal.js`
- `php spark migrate`
- `php spark os:cobrancas`

## OperaÃ§Ã£o em produÃ§Ã£o

Para a cobranÃ§a automÃ¡tica funcionar continuamente fora do fluxo manual da baixa, o servidor deve executar periodicamente:

```bash
php spark os:cobrancas
```

Agendamento recomendado em Linux/VPS:

```bash
*/15 * * * * cd /var/www/sistema-hml && php spark os:cobrancas >> writable/logs/os-cobrancas.log 2>&1
```

## ObservaÃ§Ã£o

A validaÃ§Ã£o visual com browser local embutido ficou limitada nesta sessÃ£o. A conferÃªncia final foi complementada por revisÃ£o de HTML, rotas, lint sintÃ¡tico e execuÃ§Ã£o da migration.
